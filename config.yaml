# 鼠标共享配置文件
version: 1.0

# 网络设置
network:
  server:
    host: 0.0.0.0
    port: 41234
  
  client:
    server_host: 192.168.31.199  # 修改为服务器的 IP 地址
    server_port: 41234

# 屏幕切换设置
screen_switch:
  edge_threshold: 5      # 边缘触发阈值（像素）
